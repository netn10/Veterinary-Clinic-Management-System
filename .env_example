# Environment Variables Configuration
# Copy this file to .env and update the values according to your setup

# Server Configuration
PORT=3001

# Database Configuration (PostgreSQL)
DB_USER=postgres
DB_HOST=localhost
DB_NAME=veterinary_clinic
DB_PASSWORD=password
DB_PORT=5432

# Optional: Override default values for different environments
# For production, use secure passwords and appropriate database credentials
# For development, you can use the default values above
